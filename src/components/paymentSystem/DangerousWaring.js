import { Alert } from '@mui/material';

export default function DangerousWaring() {
    return (
        <Alert severity="error">
            BE CAREFUL ! NEVER ADD A REAL CREDIT CARD INFORMATION
        </Alert>
    )
}
