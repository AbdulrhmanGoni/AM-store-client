import { Alert } from '@mui/material';
import React from 'react';

const Massege = ({ button }) => {
    return (
        <Alert variant="outlined" severity="warning">
            This is a warning alert — check it out!
        </Alert>
    );
}

export default Massege;
